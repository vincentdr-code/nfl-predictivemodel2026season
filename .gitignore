# Environments
.venv/
venv/
env/
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Raw PBP cache is huge (200MB+) and regenerable — don't commit
data/raw/
!data/raw/.gitkeep

# Processed data + trained models + outputs ARE committed so the dashboard works
# on Streamlit Cloud without needing to rebuild the pipeline

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Streamlit
.streamlit/secrets.toml

# Turso local replica cache (regenerated by libsql-experimental)
data/processed/gamification.turso.db
data/processed/gamification.turso.db-*
data/processed/gamification.db

# Misc
*.log
requirements_current.txt
